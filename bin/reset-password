#!/bin/bash
# Wrapper for reset_password.py script

# Ensure we are in the project root
cd "$(dirname "$0")/.."

# Activate virtual environment
source venv/bin/activate

# Run the python script
python3 src/scripts/reset_password.py "$@"
