#!/bin/bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

echo "==================================================="
echo "  RPS - Docker Runtime (localhost)                 "
echo "==================================================="
echo ""
echo "RPS is now intended to run via Docker Compose."
echo ""

exec "${ROOT_DIR}/bin/docker-setup"
