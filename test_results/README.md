# Test Results Artifacts

This directory is used for locally generated test and scan outputs.

- Examples: `comprehensive_test_report_*.txt`, `bandit_*.json`, lint reports.
- These files are intentionally gitignored to keep the repository focused on source code and stable documentation.

Keep only this `README.md` tracked.
