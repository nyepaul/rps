# RPS Production Environment Variables
# Copy this file to /var/www/rps.pan2.app/.env and update with your values

# Flask Configuration
SECRET_KEY=change-this-to-a-random-secret-key-in-production
FLASK_ENV=production

# Data Encryption (REQUIRED in production)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# CORS (Optional - comma-separated list of allowed origins)
CORS_ORIGINS=https://rps.pan2.app

# NOTE: API keys for AI services (Gemini/Claude) are NOT stored here.
# Each user provides their own API keys through the Settings page in the app.
# API keys are encrypted and stored per-user in the Profile database record.
